<?xml version="1.0" encoding="UTF-8"?>
<sitemesh>
	<!-- 默认装饰器，当下面的路径都不匹配时，启用该装饰器进行装饰 -->
	<mapping path="/*" decorator="/WEB-INF/views/layouts/default_decorator.jsp" />
	
	<!-- 指明满足“/exclude.jsp*”的页面，将被排除，不被装饰
	<mapping path="/login" exclue="true" /> -->
</sitemesh>